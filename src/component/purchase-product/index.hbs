{{#with data}}

<div class="product-card">
    <img src="{{img}}" alt={{title}} class="product-card__image">
    <h2 class="product-card__title">{{title}}</h2>
    <p class="product-card__subtitle">{{description}}</p>

    {{#each category}}
        <span class="product-card__category product-card__category--{{id}}">{{text}}</span>
    {{/each}}
    <a href="/purchase-product?id={{id}}">
        {{>button text="Купити зараз" price=price}}
    </a>
</div>
{{/with}}


{{!-- 
<div class="product-card">
    <img src="" alt="Purch1" class="product-card__image">
    <h2 class="product-card__title"></h2>
    <p class="product-card__subtitle"></p>

    
        <span class="product-card__category product-card__category--"></span>
    
    <a href="/purchase-product?id=" class="product-card__button">Купити зараз:  грн</a>
</div> --}}
